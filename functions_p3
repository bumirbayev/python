# assdasd
def tip_amount(tip_percentage, total):
    print("The total is: " + str(total))
    print("The tip percentage is: " + str(tip_percentage * 100) + "%")
    print("The tip amount will be: " + str(total * tip_percentage))
    return total * tip_percentage + 5

# after running in Python console, we have to declare potato variable 100, and see the changes

def buy_produce(num_of_tomatoes, num_of_bananas, num_of_potatoes):
    total_price = num_of_tomatoes * 2.00 + num_of_bananas * 1.50 + num_of_potatoes* 3.00
    print("Welcome to my Groceries")
    print("Today you bought: ")
    print(str(num_of_tomatoes) + "tomatoes")
    print(str(num_of_bananas) + " bananas")
    print(str(num_of_potatoes) + " potatoes")
    print("Your total is: $" + str(total_price))
    tips = tip_amount(0.15, total_price)
    print("Please tip me $" + str(tips))

    tips_more = tip_amount(0.5, total_price)
    print("if you're felling rich, give me $" + str(tips_more))

    # how to make it more modular? because None is given, we comment lines 3:5
    # we define the functions, instructions, we specify the inputs and name of the functions
    # functions will be used everywhere, so our functions are reusable, modular and organized
